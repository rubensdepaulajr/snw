@model IEnumerable<br.com.totaltiss.Model.Atz_Tpl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Tpl_Dsc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Tpl_Dsc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdTpl }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdTpl }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdTpl })
        </td>
    </tr>
}

</table>
